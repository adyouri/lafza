import os

SQLALCHEMY_DATABASE_URI = os.getenv('TESTING_DATABASE_URI')
SQLALCHEMY_TRACK_MODIFICATIONS = False
SECRET_KEY = 'kdljsfklfhsdfh'
JWT_ACCESS_LIFESPAN = {'seconds': 1}
JWT_REFRESH_LIFESPAN = {'seconds': 2}
RATELIMIT_DEFAULT = "600 per day, 120 per hour"
